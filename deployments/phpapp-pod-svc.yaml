apiVersion: v1
kind: Pod
metadata:
  creationTimestamp: null
  labels:
    run: phpapp-pod
  name: phpapp-pod
spec:
  containers:
  - image: fir3eye/php-app:v10
    name: phpapp-pod
    resources: {}
  dnsPolicy: ClusterFirst
  restartPolicy: Always
status: {}
--- 
apiVersion: v1
kind: Service
metadata:
  creationTimestamp: null
  labels:
    run: phpapp-pod
  name: phpapp-svc
spec:
  ports:
  - port: 8020
    protocol: TCP
    targetPort: 80
  selector:
    run: phpapp-pod
  type: NodePort
status:
  loadBalancer: {}